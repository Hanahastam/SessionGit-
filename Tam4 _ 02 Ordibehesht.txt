# قطعه کدی بنویسید که فیبوناچی یک عدد را به صورت آرایه ذخیره و نمایش دهد.

let n = 10; // تعداد اعداد فیبوناچی
let fib = [0, 1];

for (let i = 2; i < n; i++) {
    fib.push(fib[i - 1] + fib[i - 2]);
}

console.log("fib:", fib);
