# قطعه کدی بنویسید که عنصر دوم تا چهارم یک آرایه را حذف و سپس عدد ۶ را به جای آن اضافه کند

let numbers = [1, 2, 3, 4, 5, 6, 7];

numbers.splice(2, 4); // حذف عناصر از اندیس 2 تا 4
numbers.push(6);      // اضافه کردن عدد ۶ به آخر آرایه

console.log("Updated array:", numbers);